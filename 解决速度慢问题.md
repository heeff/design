# K8S 部署后 FastAPI 接口运行约 2 小时后随机超时排查

现象：

- Angular + FastAPI 项目
- 后端 Python
- 本地运行测试正常
- 部署到 K8S 后，刚启动时接口非常快
- 怎么刷新测试都没问题
- 运行大约 2 小时后，开始陆续、随机出现接口请求超时
- 如果重启 Pod 后恢复正常，那么很像某种资源随着运行时间逐渐被耗尽

对于 FastAPI + SQLAlchemy + SQL Server 这种组合，优先排查：

1. 数据库连接没有正确释放 / SQLAlchemy 连接池耗尽
2. FastAPI worker / thread pool 被阻塞
3. Pod 内存持续上涨、CPU throttling、OOM
4. SQL Server 查询变慢或出现锁等待
5. K8S Service / Ingress / Load Balancer 连接问题
6. HTTP 连接、文件句柄等资源泄漏

---

## 1. 第一优先排查：数据库连接池

典型调用链：

FastAPI  
↓  
SQLAlchemy  
↓  
pymssql  
↓  
SQL Server

如果某些接口这样创建 Session：

    db = SessionLocal()

    result = db.query(...).all()

    return result

但没有：

    db.close()

就可能出现连接逐渐被占满的问题。

刚启动时：

    连接池：
    ○ ○ ○ ○ ○

运行一段时间：

    ● ● ● ○ ○

继续访问：

    ● ● ● ● ●

最后新请求进来：

    请求
      ↓
    等待数据库连接
      ↓
    一直等待
      ↓
    请求超时

表现上就可能是：

    接口 A：正常
    接口 B：正常
    接口 C：超时
    接口 D：正常
    接口 E：超时

如果重启 Pod：

    SQLAlchemy Engine / Connection Pool 重新初始化
      ↓
    原有连接全部释放
      ↓
    接口恢复正常

然后过一两个小时再次出现问题。

---

## 2. 检查 FastAPI Session 是否正确关闭

推荐写法：

    def get_db():
        db = SessionLocal()
        try:
            yield db
        finally:
            db.close()

接口：

    @router.get("/api/test")
    def test(db: Session = Depends(get_db)):
        result = db.query(...).all()
        return result

关键是：

    finally:
        db.close()

这样无论接口是：

- 正常 return
- 抛异常
- 中途退出

最终 Session 都会关闭，连接会归还给连接池。

如果项目中大量存在：

    db = SessionLocal()

或者：

    session = Session()

但没有：

    db.close()

或者：

    session.close()

需要重点检查。

---

## 3. 检查 SQLAlchemy Engine 连接池配置

例如：

    engine = create_engine(
        DATABASE_URL,
        pool_size=10,
        max_overflow=20,
        pool_timeout=30,
        pool_recycle=1800,
        pool_pre_ping=True
    )

参数含义：

### pool_size

    pool_size=10

表示连接池长期保留最多 10 个连接。

### max_overflow

    max_overflow=20

高峰时允许额外创建 20 个连接。

因此理论最大连接数：

    10 + 20 = 30

如果这 30 个连接全部被占用，新请求只能等待。

### pool_timeout

    pool_timeout=30

表示等待数据库连接最多 30 秒。

如果 30 秒仍拿不到连接，可能出现类似错误：

    QueuePool limit of size 10 overflow 20 reached
    connection timed out

或者在前端 / Ingress 层表现成：

    504 Gateway Timeout

### pool_pre_ping

建议：

    pool_pre_ping=True

SQLAlchemy 每次从连接池取连接时，会先检查连接是否仍然可用。

这对 SQL Server、长连接、网络设备主动断开空闲连接等情况比较有帮助。

### pool_recycle

例如：

    pool_recycle=1800

表示连接使用到一定时间后重新创建。

1800 秒 = 30 分钟。

可以避免某些长期连接失效的问题。

但需要注意：

    pool_recycle 不能解决 Session 没有 close 导致的连接泄漏。

如果程序一直拿连接不归还，设置 recycle 并不能从根本上解决。

---

## 4. 检查 Pod CPU 和内存

系统刚部署、访问很快时执行：

    kubectl top pod -n <namespace>

记录 CPU 和 MEMORY。

例如：

    NAME            CPU     MEMORY
    backend-xxxxx   35m     180Mi

等两小时左右开始出现接口超时后，再执行：

    kubectl top pod -n <namespace>

如果变成：

    NAME            CPU     MEMORY
    backend-xxxxx   900m    950Mi

就需要重点检查 CPU 或内存。

尤其如果内存类似：

    启动：
    180 MB

    30 分钟：
    260 MB

    1 小时：
    390 MB

    2 小时：
    650 MB

    3 小时：
    900 MB

很可能存在：

- 内存泄漏
- 缓存无限增长
- 对象没有释放
- 数据库查询结果长期保存
- 全局 list / dict 不断增加
- 日志缓冲
- 后台任务堆积

---

## 5. 检查 Pod 是否发生过重启

执行：

    kubectl get pods -n <namespace>

重点看：

    RESTARTS

例如：

    NAME             READY   STATUS    RESTARTS
    backend-xxx      1/1     Running   3

如果 RESTARTS 在增加，再执行：

    kubectl describe pod backend-xxx -n <namespace>

重点查看是否有：

    OOMKilled

或者：

    Reason: OOMKilled
    Exit Code: 137

如果有 OOMKilled，可能是：

    内存持续上涨
      ↓
    达到 K8S memory limit
      ↓
    Container 被 kill
      ↓
    Pod / Container 重启

---

## 6. 检查 FastAPI 是否存在同步阻塞

例如：

    @app.get("/api/test")
    async def test():
        result = blocking_database_query()
        return result

虽然接口写的是：

    async def

但如果内部使用的是：

- pymssql
- SQLAlchemy 同步 Engine
- requests
- 其他同步阻塞函数

它们本质上还是同步调用。

如果某个 SQL 平时：

    0.2 秒

但偶尔：

    30 秒

就可能长期占用 worker / thread。

当慢请求越来越多：

    Worker 1 -> 等数据库
    Worker 2 -> 等数据库
    Worker 3 -> 等数据库
    Worker 4 -> 等数据库

最后表现为：

    随机接口超时

---

## 7. 检查 FastAPI Worker 数量

如果 K8S 中启动方式只是：

    uvicorn main:app

或者：

    uvicorn main:app --host 0.0.0.0 --port 8000

通常就是单 worker。

生产环境可能配置：

    uvicorn main:app \
      --host 0.0.0.0 \
      --port 8000 \
      --workers 4

但 worker 不是越多越好。

因为 SQLAlchemy Connection Pool 通常是：

    每一个 Python Process
    都有自己独立的连接池

例如：

    workers = 4
    pool_size = 10

理论上基础连接数可能达到：

    4 × 10 = 40

如果同时有 3 个 Pod：

    3 Pod
    × 4 workers
    × 10 connections

    = 120 个连接

如果再考虑：

    max_overflow=20

理论峰值连接数还会更多。

所以必须综合考虑：

- K8S Pod 数
- Uvicorn / Gunicorn Worker 数
- SQLAlchemy pool_size
- SQLAlchemy max_overflow
- SQL Server 最大连接能力

---

## 8. 最有价值的定位测试

下一次接口开始超时时：

不要立刻重启 Pod。

先执行：

    kubectl top pod -n <namespace>

    kubectl get pods -n <namespace>

    kubectl logs <backend-pod> -n <namespace> --tail=500

    kubectl describe pod <backend-pod> -n <namespace>

同时分别测试：

1. 一个完全不访问数据库的接口
2. 一个需要访问 SQL Server 的接口

可以临时增加一个最简单的接口：

    @app.get("/health-test")
    def health_test():
        return {"status": "ok"}

然后在故障发生时测试。

如果：

    /health-test       10ms
    /database-api      timeout

说明 FastAPI 自身还活着。

重点排查：

- SQL Server
- SQLAlchemy
- 数据库连接池
- 慢 SQL
- 数据库锁
- 网络到数据库的连接

如果：

    /health-test       timeout
    /database-api      timeout

则更偏向：

- FastAPI Worker 被耗尽
- Thread Pool 被耗尽
- CPU 满
- Pod 内存问题
- Python 进程卡死
- K8S 网络
- Ingress / Service
- 文件句柄等系统资源耗尽

---

## 9. 推荐排查流程

    接口运行约 2 小时后随机超时
                  |
                  v
          重启 Pod 是否恢复？
             /           \
           是             否
           |               |
           v               v
      优先怀疑         排查外部服务/
      资源耗尽         网络/Ingress
           |
      +----+----+
      |         |
      v         v
    DB连接     CPU/内存
      |         |
      v         v
    Session   kubectl top
    Pool      pod
      |
      v
    慢 SQL /
    DB锁 /
    连接池耗尽

另外还需要检查：

    Worker / Thread Pool
           |
           v
    是否有同步阻塞调用

---

## 10. 当前优先级判断

根据下面这些特征：

- 本地运行正常
- K8S 刚部署时非常快
- 刷新测试没有问题
- 大约运行 2 小时后才出现
- 不是所有接口同时坏
- 而是陆续、随机超时

第一优先怀疑：

    SQLAlchemy Session 没有正确释放
            ↓
    数据库连接逐渐被占用
            ↓
    Connection Pool 耗尽
            ↓
    新请求等待连接
            ↓
    随机超时

第二优先怀疑：

    FastAPI Worker / Thread Pool 被慢 SQL 或同步操作阻塞

第三优先怀疑：

    Pod CPU / Memory 持续增长

第四优先怀疑：

    SQL Server 锁等待 / 慢查询 / 长事务

第五优先才考虑：

    K8S Service / Ingress / Load Balancer 网络问题

最关键的是：

    故障再次发生时不要马上重启 Pod。

应该趁故障还存在时收集：

    kubectl top pod
    kubectl logs
    kubectl describe pod
    Pod restart 次数
    CPU
    Memory
    不访问 DB 的接口响应时间
    访问 DB 的接口响应时间
    SQLAlchemy 连接池日志

这些信息基本可以把问题范围迅速缩小。